---
description: Audit a codebase for shallow modules and tangled boundaries, stage deepening changes, and emit a self-contained HTML report.
argument-hint: [target path, optional — defaults to the whole repository]
---

Invoke the **`engineering:improve-codebase-architecture`** skill and follow it exactly: one
scan, one pass through its findings, one HTML report.

Target: $ARGUMENTS

If no target was provided above, scan the whole repository.
